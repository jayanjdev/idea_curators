<%= simple_form_for(@profile) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nickname %>
    <%= f.cktext_area :objective %>
    <%= f.cktext_area :offerings %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
